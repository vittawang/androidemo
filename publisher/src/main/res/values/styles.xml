<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LibraryDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/tran</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="LibraryAnimFade_two" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/library_dialog_in_two</item>
        <item name="@android:windowExitAnimation">@anim/library_dialog_out_two</item>
    </style>

</resources>
